# Symbols to force-export that are not discoverable from EDK public headers.
#
# Zephyr API symbols (kernel, networking, USB, etc.) are auto-discovered
# from EDK headers by gen_auto_exports.py and kept alive via --no-gc-sections
# in pass 1, so they do NOT need to be listed here.
#
# Line prefixes:
#   ~   regex pattern (matched against all ELF symbols)
#   ?   optional/board-specific (weak extern — no error if missing)
#   (none) concrete symbol (--undefined pulls from archives)
#
# Lines starting with '#' are comments. Blank lines are ignored.

# --- Compiler runtime (regex patterns) ---
~^__aeabi_
~^__cxa_
~^__stack_chk_

# --- Zephyr assert ---
__assert_no_args

# --- Zephyr logging (loader-defined symbols, not in EDK headers) ---
log_const_sketch
log_dynamic_sketch
z_log_msg_runtime_vcreate

# --- Libc: string.h ---
memcpy
memmove
strrchr
strstr
strncmp
strncpy
strcasecmp
strcmp
strlen
strchr
strcat
strcpy
memcmp
memset

# --- Libc: stdlib.h ---
malloc
realloc
calloc
free
rand
srand
strtod
strtol
strtoul
atoi
atof
atol
abort
exit
atexit
_exit

# --- Libc: ctype.h ---
isspace
isalnum
tolower
toupper
isalpha
iscntrl
isdigit
isgraph
isprint
isupper
islower
isxdigit

# --- Libc: math.h ---
acos
acosf
asin
asinf
atan
atan2
atan2f
atanf
cos
cosf
exp
exp2
log
logf
log2
log10
pow
sin
sinf
sqrt
sqrtf
tan
tanf
ldexp

# --- Libc: stdio.h ---
puts
putchar
vsnprintf
printf
sprintf
snprintf
sscanf

# --- Libc: time.h ---
time
mktime

# --- Libc: other ---
cbvprintf

# --- Variables (STT_OBJECT, not picked up by function-only scan) ---
stdin
stdout
stderr

# --- Board-specific (optional — only exist on certain boards) ---
?matrixBegin
?matrixWrite
?matrixPlay
?matrixGrayscaleWrite
?matrixSetGrayscaleBits
?matrixEnd
