# SPDX-License-Identifier: Apache-2.0
#
# Arduino debug configuration for RP2040.
# Targets core 0 only (where the Arduino sketch runs) and sets a safe
# default adapter speed. Wraps the stock OpenOCD 0.12 target script.

set USE_CORE 0
source [find target/rp2040.cfg]
adapter speed 2000
